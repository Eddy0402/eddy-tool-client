function LoadPlugin(GM, Config){
    var defaultConfig = {
        base         : 'http://agario.itseddy.me/',
        server_url   : '',
    };
    if(!Config){
        Config = defaultConfig;
    }
}
